#include<stdio.h>
#include<string.h>

int main()
{
    char s[1000];
    char c,len;
    scanf("%s", &s);

    len=strlen(S);

    for(int i=0; i<len; i++)
    {

    }
    printf("%s", s);

    return 0;
}
