# include<stdio.h>

int main() {
    float number;
    printf("enter number = ");
    scanf("%f", &number);

    //cuben formula (number*number*number) //

    printf("It's cuben is : %f", number*number*number);



    return 0;
}